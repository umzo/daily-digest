import { describe, it } from 'vitest'

describe('Article', () => {
  it.todo('必須フィールド (id, source, title, content, url, publishedAt) を持つ')
  it.todo('オプションフィールド (author, tags) は省略可能')
})

describe('Fetcher', () => {
  it.todo('name プロパティを持つ')
  it.todo('fetch() メソッドで Article[] を返す')
})
